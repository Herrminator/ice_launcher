#!/usr/bin/env python3
import os
import ice_launcher.main

if os.environ.get("FFMPEG_BIN") is not None:
    os.environ["PATH"] = os.pathsep.join([os.environ["FFMPEG_BIN"], os.environ["PATH"]])

ice_launcher.main.main()
